###############################################################################
# ublue-os/brew — the prebuilt Homebrew tarball
###############################################################################
# Stages the Homebrew tree so it exists on first boot without a download.
# The docker source pins the per-architecture manifest digest, not the
# multi-arch index digest, so each arch carries its own ref; track with
#   just bst -o arch <arch> source track runtime/brew-tarball.bst
###############################################################################
kind: manual

sources:
- kind: docker
  url: ghcr:ublue-os/brew
  track: latest
  (?):
  - arch == "x86_64":
      ref: 926ce25a12476755bf53ac706e7d519d40946e7fefa3b1838920f259a43821ee
  - arch == "aarch64":
      ref: aff477fa0ccbbb467946ed12e1ec6cd7726281e555daa152ef64af8f3106a5e6

build-depends:
- core/sandbox-tools.bst
- freedesktop-sdk.bst:public-stacks/runtime-minimal.bst

runtime-depends:
- runtime/brew.bst

variables:
  strip-binaries: ""

config:
  install-commands:
  - install -Dm644 system_files/usr/share/homebrew.tar.zst "%{install-root}%{datadir}/homebrew.tar.zst"
  - "%{install-extra}"
